spring:
  application:
    name: rentalapi

  datasource:
    url: jdbc:mysql://localhost:3306/rentaldb
    username: rental_user
    password: managerental
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8084

api:
  security:
    token:
      secret: minha-chave-super-secreta-123
